#include <stdlib.h>
#include <time.h>
/* print the last digit of the number stored in the variable n */

/* The variable n will store a different value every time this program is runned */
int main(void)
{
	int n;
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	printf("Last digit of/n")
		("n")
		("is")
		if (lastDigitofn > 5)
			printf("and is greater than 5")
				if (lastDigitofn = 0)
					printf("and is 0")
						if (lastDigitofn < 6 not 0(
									prinf("and is less than 6 and not 0")
	return (0);
}
