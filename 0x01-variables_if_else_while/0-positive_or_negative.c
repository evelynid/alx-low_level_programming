#include <stdio.h> 
#include <time.h>
/* assign a random number to the variable n each time it is executed */

/* store a different value every time this program is runned */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n >  0 = +1)
		(n = 0 = 0)
			(n < 0 = -1)
	return (0);
}
