#include <stdlib.h>
#include <time.h>
/* prints all single digit numbers of base 10 starting from 0 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	for (i = 0; < 10; i++)
	{
		printf("%i", i);
	}
}
	printf("\n");
	return (0);
